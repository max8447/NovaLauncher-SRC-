<UserControl x:Class="NovaLauncher.Views.Controls.LaunchProgressPopup"
             xmlns:local="clr-namespace:NovaLauncher.Views.Controls"
xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="300">
    <Grid>
        <ui:Card VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" Padding="8,4,8,4" Background="#FF2E2E2E">
            <Grid>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="PopupText" Grid.Row="0" Text="Please wait as we log you in!" VerticalAlignment="Center" Margin="1,4,0,1" TextAlignment="Center" HorizontalAlignment="Center"/>

                    </StackPanel>
                    <StackPanel Grid.Row="1" Margin="0,12,0,0">
                        <Border Height="16">
                            <ProgressBar x:Name="ProgressBar" IsIndeterminate="True"/>
                        </Border>
                    </StackPanel>
                </StackPanel>

            </Grid>
        </ui:Card>
    </Grid>
</UserControl>
